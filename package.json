{
  "name": "contact-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-only Google and Microsoft contacts sync",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "check": "node scripts/build.js --check"
  },
  "engines": {
    "node": ">=20"
  }
}
