# Local configuration with your own client IDs (copy from config.example.js).
# Client IDs are public OAuth identifiers, but keeping this out of git keeps the repo generic.
config.js

# Generated production output
dist/

# Editor / OS noise
.DS_Store
*.log
node_modules/
